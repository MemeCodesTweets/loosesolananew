import TriangularImageScroller from "./imageScroll";
import ImageScroller from "./imageScroll";

export default function ImageScrollerWrapper() {
  return (
    <main className="flex items-center  justify-center">
      <TriangularImageScroller />
    </main>
  )
}
